"""PantryChef — a no-LLM multimodal cooking assistant.

Phases (see ROADMAP.md):
    1. data + ingredients + retrieval (foundation)
    2. substitution (flagship: embeddings + graph)
    3. recommender (LTR + two-tower)
    4. nutrition (deterministic + optional imputation)
    5. vision (detection → ONNX → edge)
    6. serving (FastAPI + hosted demo)
"""

__version__ = "0.0.0"
