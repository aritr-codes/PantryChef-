"""Serving layer — FastAPI inference API (Phase 6).

Placeholder. Wraps the pipeline (ingredients -> retrieval -> substitution ->
recommender -> nutrition) behind an API; deployed to a free hosted demo.
"""
